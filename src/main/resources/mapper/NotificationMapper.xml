<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssginc.unnie.notification.mapper.NotificationMapper">

    <insert id="insertNotification">
        INSERT INTO
        notification
        (
         notification_type,
         notification_contents,
         notification_urn,
         notification_member_id
        )
        VALUES
        (
         #{notificationType},
         #{notificationContents},
         #{notificationUrn},
         #{notificationMemberId}
        )
    </insert>

    <select id="getMyNotificationsByMemberId"
            resultType="com.ssginc.unnie.notification.dto.NotificationMessage">
        SELECT
            notification_id,
            notification_isread,
            notification_contents,
            notification_urn
        FROM
            `notification`
        WHERE
            notification_member_id = #{memberId}
          AND
            notification_isread = 0
        ORDER BY
            notification_created_at DESC
            LIMIT 5
    </select>

    <select id="getAllMyNotificationsByMemberId"
            resultType="com.ssginc.unnie.notification.vo.Notification">
        SELECT
            notification_id,
            notification_type,
            notification_contents,
            notification_created_at,
            notification_urn,
            notification_isread
        FROM
            `notification`
        WHERE
            notification_member_id = #{memberId}
        ORDER BY
            notification_created_at DESC
    </select>

    <update id="updateIsRead">
        UPDATE notification
        SET notification_isread = 1
        WHERE notification_id = #{notificationId}
    </update>

    <update id="updateAllIsReadByMemberId">
        UPDATE notification
        SET notification_isread = 1
        WHERE notification_member_id = #{memberId}
          AND notification_isread = 0
    </update>

    <select id="countUnreadNotifications" resultType="int">
        SELECT COUNT(*)
        FROM notification
        WHERE notification_member_id = #{memberId}
          AND notification_isread = 0
    </select>

    <delete id="deleteNotifications">
        DELETE FROM notification
        WHERE notification_id IN
        <foreach item="id" collection="list" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <delete id="deleteAllNotificationsByMemberId">
        DELETE FROM notification
        WHERE notification_member_id = #{memberId}
    </delete>
</mapper>